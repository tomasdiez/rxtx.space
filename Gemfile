source "https://rubygems.org"

# GitHub Pages builds this site natively with a locked, whitelisted plugin set.
# The github-pages gem mirrors that exact environment locally.
gem "github-pages", group: :jekyll_plugins

# Ruby 3 no longer ships WEBrick; `jekyll serve` needs it locally.
gem "webrick", "~> 1.8"
